你是一个专业的软件工程师。下面是一个 GitHub issue 中报告的 bug。请为该 bug 生成一个最小化、可直接运行的代码复现（MRE）。

要求如下：

0. 先根据 issue 描述判断复现类型：
   - 如果 bug 明确依赖特定版本、运行时、操作系统、配置、外部服务，或者必须调用真实库行为才能触发，请生成“真实依赖复现”。
   - 如果 bug 的核心可以在不依赖外部服务的情况下稳定复刻，请优先生成“最小化复现”。
   - 选定的复现类型必须在 `README.md` 中明确说明，不能让使用者猜测。

1. 生成一个独立的代码仓库结构，包含：
   - 一个 `reproduce.py`（或对应语言的主文件），可直接运行触发 bug。
   - 一个 `README.md`，其中必须包含明确的“复现步骤”（step-by-step），按顺序列出从克隆/创建文件到看到错误输出的完整命令或操作。
   - 如果需要，包含 `requirements.txt`、`Dockerfile`、`package.json` 等依赖声明文件。

2. 代码必须：
   - 尽量精简，只保留触发 bug 的必要逻辑。
   - 不依赖外部网络服务或不可保证存在的文件（除非 issue 中明确要求，并提供模拟数据或 mock）。
   - 能独立运行，无需额外猜测。

3. README 中的“复现步骤”必须包含：
   - 环境准备（语言/依赖版本）
   - 安装依赖的命令
   - 运行代码的具体命令
   - 预期结果（无 bug 时应输出的内容）
   - 实际结果（运行后看到的错误输出或异常行为）

4. 如果 bug 与特定环境（操作系统、版本、配置）相关，请明确说明。

请输出完整的文件内容，每个文件以文件名作为标题，例如：

### `README.md`

（内容）

### `reproduce.py`

（内容）

...

现在请基于以下 issue 生成复现：

[粘贴 issue 链接或完整描述]
